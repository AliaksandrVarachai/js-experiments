.header {
  border: 1px solid black;
  padding: 5px 10px;

  &:hover {
    cursor: pointer;
  }
}

.header--opened {
  max-width: var(--max-menu-width);
  overflow: hidden;
  white-space: nowrap;
  text-overflow: ellipsis;
}
